#include "main.h"

/**
 *_strlen - function that returns len of string
 *@s: variable of function
 *Return: length of string
 */

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{}
return (i);
}
